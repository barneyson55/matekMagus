# Testing

## Commands (Windows)
- TBD: document exact `npm` commands for unit + E2E on Windows.

## Commands (WSL / Headless)
- TBD: document exact `npm` commands and expected E2E skip behavior.

## Automation Policy
- Autopilot must not run tests; only generate/update them.
