# User TODO

- [ ] Confirm the Codex CLI command/flags for non-interactive runs (autopilot).
- [ ] Confirm the Codex CLI command/flags for read-only review runs.
- [ ] Confirm next module priority after `paritas` (default: `fuggveny_transzformaciok`).
- [ ] Provide or confirm the app icon file for `assets/icon.png` (or desired path).
- [ ] Decide whether to auto-generate navigation from `assets/temakorok` or keep manual lists.
